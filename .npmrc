# puppeteer is a devDependency for scripts/e2e only, and those scripts reuse a
# Chromium that Playwright already has on disk (see scripts/e2e/env.mjs).
# Without this, every `npm install` pulls a ~150MB browser nobody uses.
puppeteer_skip_download=true
